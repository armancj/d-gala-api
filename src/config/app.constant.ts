export const appConstant = {
  STATE: 'STATE',
  HOST: 'HOST',
};

export const enum statusState {
  development = 'dev',
  production = 'prod',
  test = 'test',
  provision = 'prov',
}

export const enum prismaTable {
  colors = 'colors',
}
